# Notas
Primer programa en node.

```
Options:
      --help     Show help                                                  [boolean]
      --version  Show version number                                        [boolean]
  -b, --base     Nos permite definir la base de la tabla de multiplicar.    [number] [required]
  -l, --listar   Nos muestra la salida de la aplicacion sin abrir el txt.   [boolean] [default: "false"]
  -h, --hasta    Nos permite definir el limite de la tabla de multiplicar.  [number] [default: 10]
```